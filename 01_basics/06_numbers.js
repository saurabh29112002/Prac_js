// Basically this program is used to calculate the random number between given min and max

const min = 10
const max = 20

console.log((Math.floor(Math.random() * (min - max + 10)) + min));


const Mynumber = new Number(100)
console.log(Mynumber);

// need to add more on numbers